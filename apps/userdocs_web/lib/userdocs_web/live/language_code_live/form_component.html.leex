<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "language_code-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :code %>
  <%= text_input f, :code %>
  <%= error_tag f, :code %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
