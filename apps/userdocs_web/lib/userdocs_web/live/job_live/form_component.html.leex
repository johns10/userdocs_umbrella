<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "job-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :job_type %>
  <%= text_input f, :job_type %>
  <%= error_tag f, :job_type %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
