defmodule UserDocs.Endpoint do
  @moduledoc """
  This module defines test helpers for creating
  entities via the `UserDocs.Auth` context.
  """

  def broadcast(thing) do
    IO.inspect(thing)
  end
end
